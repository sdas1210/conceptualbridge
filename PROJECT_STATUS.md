# Conceptual Bridge Project Status Update

## Added: 2026-08-07

# Developer Maintenance Suite Progress Update (Added: 2026-08-07 --- Maintenance Framework v2.1 Frozen)

## Current Status

**Version:** Developer Maintenance Suite v2.1 (Shared Framework Frozen)

### Completed This Session

#### Shared Maintenance Framework

-   ✅ Shared framework architecture established.
-   ✅ Configuration-driven architecture adopted.
-   ✅ Shared utility modules introduced.
-   ✅ Theme, Logger, Storage and Helper framework established.
-   ✅ Template framework separated from application logic.
-   ✅ Version management introduced.
-   ✅ Framework bootstrap architecture completed.
-   ✅ Maintenance tools prepared for future reuse through shared
    modules.
-   ✅ Architecture reviewed and frozen.

#### YouTube Anchor Builder

-   ✅ Wizard workflow completed.
-   ✅ Live preview architecture completed.
-   ✅ Configuration-driven metadata generation completed.
-   ✅ Import / Export architecture completed.
-   ✅ Builder architecture refactored into reusable modules.
-   ✅ Production architecture review completed.

### Engineering Decision

The Maintenance Framework is now considered **architecturally frozen**.

Future maintenance tools must reuse the shared framework instead of
implementing duplicate helper logic.

Only bug fixes and feature additions are permitted.

No further architectural refactoring is planned at this stage.

------------------------------------------------------------------------

# Immediate Priority --- Next Development Session

## Phase 1 --- Anchor Builder Integration

**Status:** Ready to Begin

The current Anchor Builder architecture has been completed but now
requires full end-to-end integration.

### Objectives

1.  Complete integration of the GACA Anchor Builder.
2.  Complete implementation of the Mathematics Anchor Builder.
3.  Verify every shared module is actively used.
4.  Verify live preview generation.
5.  Verify metadata generation.
6.  Verify TXT download workflow.
7.  Verify TXT import workflow.
8.  Complete production testing.

### Engineering Principle

No Runtime Integration will begin until the Anchor Builder workflow is
fully operational.

The Anchor Builder becomes the official producer of standardized anchor
files consumed by the Knowledge Runtime.

------------------------------------------------------------------------

# Runtime Integration Roadmap

After successful completion of the Anchor Builder integration:

## Phase 2 --- Runtime Integration

Planned implementation order:

1.  `gacaAnchorParser.js`
2.  `mathAnchorParser.js`
3.  Runtime Anchor Loader
4.  Knowledge Library Integration
5.  Quiz Portal Integration
6.  End-to-End Runtime Validation

### Engineering Principle

The runtime must consume standardized artifacts generated by the
Maintenance Suite.

No runtime component should duplicate maintenance logic.

------------------------------------------------------------------------

# Development Environment

The project now has access to multiple AI assistants that may be used
when appropriate.

Available assistants:

-   ✅ ChatGPT (Primary Architecture, Planning, Integration and Code
    Review)
-   ✅ Gemini Pro
-   ✅ Qwen
-   ✅ Claude (Limited Free Version)

### Engineering Guideline

The available AI assistants may be used whenever appropriate for
implementation, review or comparison.

All generated code must ultimately conform to the Conceptual Bridge
architecture, coding standards and project roadmap before being merged
into the master codebase.

------------------------------------------------------------------------

# Development Timeline (Updated)

  Date         Milestone
  ------------ ---------------------------------------------------
  2026-08-01   Global Metadata Tagger Production Release
  2026-08-04   Question Format Converter Production Release
  2026-08-06   Knowledge Index Engine v1.0 (Architecture Frozen)
  2026-08-07   Maintenance Framework v2.1 (Architecture Frozen)
  2026-08-07   YouTube Anchor Builder Architecture Completed
  Next         Anchor Builder Integration (GACA & Mathematics)
  Next         Runtime Integration Phase

------------------------------------------------------------------------

# Current Continuation Point

Resume development from:

**Phase 1 --- Anchor Builder Integration**

Focus exclusively on completing and validating the GACA and Mathematics
Anchor Builders before beginning Runtime Integration.

------------------------------------------------------------------------

# Project Status Update --- 2026-08-09

## Anchor Builder Stabilization Completed

### Session Outcome

The YouTube Anchor Builder was tested and the previously identified
Level-1 initialization and wizard workflow issues were resolved.

The Anchor Builder is now considered **functionally operational and
frozen for the current phase**.

### Issues Resolved During This Phase

#### 1. Operation Mode Rendering

The initial Operation Mode screen was not displaying the available
modes.

The underlying cause was traced through the ES-module dependency chain
and corrected.

The builder now correctly exposes:

-   GACA --- Active
-   MATH --- Active
-   GS --- Inactive / Coming Soon
-   GI --- Inactive / Coming Soon

#### 2. Sequential Metadata Preview

Topic, Sub-Topic and Level were initially being emitted during preview
initialization even though their wizard stages had not yet been
completed.

The metadata compilation logic was refined to use wizard completion
state.

Current behavior:

``` text
Before Step 10:
Topic|             not shown
Sub-Topic|         not shown
Level|             not shown

After Step 10 is saved:
Topic|             shown

After Step 11 is saved:
Topic|
Sub-Topic|

After Step 12 is saved:
Topic|
Sub-Topic|
Level|
```

Blank values are intentionally preserved once their corresponding stages
have been saved.

#### 3. Step 13 Output / Download Workflow

The output filename stage previously lacked a functional save action,
preventing the download workflow from becoming active.

Step 13 now provides:

``` text
Filename Identifier
[ value ] .txt

[ Save & Prepare Download ]

[ Download Anchor File (.txt) ]
```

The expected workflow is:

``` text
Enter filename identifier
        ↓
Save & Prepare Download
        ↓
Validate filename
        ↓
Complete Step 13
        ↓
Enable Download
        ↓
Download Anchor File
```

For example:

``` text
4
```

produces:

``` text
4.txt
```

The existing download mechanism was preserved rather than redesigned.

### Current Anchor Builder Status

The complete wizard flow is now operational:

``` text
GACA / MATH Selection
        ↓
Title
        ↓
Set Number
        ↓
Tags
        ↓
English / Primary YouTube
        ↓
Bengali YouTube (where applicable)
        ↓
PDF English
        ↓
PDF Bengali
        ↓
Test Source
        ↓
Topic
        ↓
Sub-Topic
        ↓
Level
        ↓
Output Filename
        ↓
Save & Prepare Download
        ↓
Download Anchor TXT
```

### Freeze Decision

**Anchor Builder current implementation: FROZEN**

No further architectural refactoring should be performed on the Builder
at this stage.

Only verified bug fixes, required compatibility fixes, and narrowly
scoped feature additions should be made.

------------------------------------------------------------------------

# Revised Next Development Step

The Anchor Builder has now reached the required operational checkpoint.

The next task is **not immediate API integration**.

The next task is:

## Anchor → Runtime Integration Validation

Before continuing Phase 3 API integration, validate the real artifact
produced by the Builder.

Required validation chain:

``` text
Anchor Builder
      ↓
Generated Anchor TXT
      ↓
Anchor Importer
      ↓
GACA Anchor Parser / Math Anchor Parser
      ↓
Runtime Engine
      ↓
Knowledge Library
      ↓
Candidate Resolution
      ↓
Question Pool
      ↓
Question Selection
```

### Required Tests

1.  Generate a complete GACA anchor TXT.
2.  Generate a complete Mathematics anchor TXT.
3.  Verify the downloaded TXT contents.
4.  Verify the TXT can be imported through `anchorImporter.js`.
5.  Verify GACA anchor normalization.
6.  Verify Math anchor normalization.
7.  Verify Runtime Engine accepts the standardized anchor artifact.
8.  Verify the Runtime produces the expected question pool.
9.  Only after successful validation, resume API integration.

------------------------------------------------------------------------

# Current Continuation Point

Resume from:

**Anchor → Runtime Integration Validation**

Do not restart Anchor Builder development unless a regression is
discovered.

Do not remove the legacy API pipeline yet.

The next milestone is to prove that a real standardized Anchor TXT
produced by the frozen Maintenance Builder can successfully travel
through the Runtime pipeline.

------------------------------------------------------------------------

# Updated Development Timeline

  Date         Milestone
  ------------ -----------------------------------------------------
  2026-08-01   Global Metadata Tagger Production Release
  2026-08-04   Question Format Converter Production Release
  2026-08-06   Knowledge Index Engine v1.0 --- Architecture Frozen
  2026-08-07   Maintenance Framework v2.1 --- Architecture Frozen
  2026-08-07   YouTube Anchor Builder Architecture Completed
  2026-08-09   Anchor Builder Functional Stabilization Completed
  Next         Anchor → Runtime Integration Validation
  Later        API Runtime Integration

------------------------------------------------------------------------

# Session End Checkpoint --- 2026-08-09

**Status: Stable checkpoint reached.**

The current Anchor Builder implementation is working smoothly and should
be treated as the frozen baseline for the next development session.

Continue from:

## \> **Anchor → Runtime Integration Validation**

# Session Update --- 2026-08-10

## Quiz Portal --- Initial Question Status Correction

### Completed

The initial question-palette state behavior in `quiz-portal.html` has
been corrected.

At the beginning of an examination:

-   **Not Visited** = Total number of questions
-   **Answered** = 0
-   **Marked** = 0
-   **Marked & Answered** = 0
-   **Not Answered** = 0

The previous behavior where the first question was automatically
converted to **Not Answered** has been corrected.

The existing later-state behavior remains unchanged: a question becomes
**Not Answered** when the candidate actually navigates away from an
unattempted question, while answered and review states continue to
operate normally.

### Scope Control

This change applies specifically to the **Quiz Portal
(`quiz-portal.html`)**.

No unrelated project architecture, roadmap, or development direction has
been changed.

### Status

**✅ Completed and verified**

The corrected behavior should be treated as the current Quiz Portal
baseline.

---

# Session Update — 2026-08-11
## Anchor → Runtime Integration Validation / Knowledge Index Assessment

### Master-Copy Preservation Rule

This document remains the **mother/master Project Status copy**.

**Important:** All previous project history, architecture decisions, completed milestones, constraints, and roadmap information above are preserved. Future updates must be appended to this master copy rather than replacing or rewriting earlier project history.

The master copy is the authoritative continuation document for future sessions.

---

## 1. Runtime Integration Validation — Current Assessment

The following existing Runtime components have now been reviewed against a real GACA Anchor TXT generated by the now-frozen YouTube Anchor Builder:

```text
developer/maintenance/shared/anchorImporter.js
services/runtime/gacaAnchorParser.js
services/runtime/mathAnchorParser.js
services/runtime/runtimeEngine.js
services/runtime/candidateResolver.js
services/runtime/questionPoolBuilder.js
services/questionParser.js
```

### Real GACA Anchor Tested

The generated GACA anchor contained:

```text
Title| 100 Most Important GK/GA Live Question Analysis (Set - 1)
Tags| RRB Group D, 10, GACA, 100-Series, CEN 08/2024
English| 8VPQawDm0gM
Bengali| WCzS7yHYUXs
PDF_English| none
PDF_Bengali| none
Test_Source| questions/gaca/1.txt
Topic|
Sub-Topic|
Level|
```

### Validation Result

The anchor structure is compatible with the current Runtime architecture.

Validated conceptual path:

```text
Real GACA Anchor TXT
        ↓
anchorImporter.js
        ↓
gacaAnchorParser.js
        ↓
candidateResolver.js
        ↓
questionPoolBuilder.js
        ↓
questionParser.js
        ↓
runtimeEngine.js
```

### Important Runtime Finding

`candidateResolver.js` correctly treats `Test_Source` as the primary source constraint when present.

For:

```text
Test_Source| questions/gaca/1.txt
```

the resolver can resolve the specific source file rather than performing broad topic discovery.

The blank Topic / Sub-Topic / Level fields therefore do not prevent this strict Test_Source flow.

### Existing Question File Validation

`questions/gaca/1.txt` is already known to work correctly through the existing Tutorial Corner.

Therefore:

- The source question file itself is valid.
- The existing question parsing/content format is operational.
- Tutorial Corner currently loads the file successfully.

The remaining Runtime work is therefore primarily **integration and contract validation**, not a redesign of the existing question format.

---

# 2. API Architecture Clarification

Two existing APIs have been distinguished and must not be treated as identical.

## `api/fetch-questions.js`

Primary role:

**Exam Corner / general dynamic question retrieval**

Current behavior is anchor-independent.

It accepts criteria such as:

```text
topic
limit
```

and can discover questions across the appropriate subject/question folders.

Conceptually:

```text
Exam Corner
    ↓
fetch-questions.js
    ↓
Subject / Topic criteria
    ↓
Multiple eligible question files
    ↓
Question selection
```

This endpoint should **not simply be replaced by `runtimeEngine.js`**.

`runtimeEngine.js` is the Runtime execution layer, while `fetch-questions.js` is an API entry point.

---

## `api/fetch-tutorial-questions.js`

Primary role:

**Tutorial Corner / anchor-driven question retrieval**

Current architecture is source-specific and accepts a specific source file.

Conceptually:

```text
Tutorial Corner
    ↓
fetch-tutorial-questions.js
    ↓
Specific source
    ↓
One TXT question file
    ↓
Select 10 questions
```

### Intended Future Rule

Tutorial Corner is intended to become **strictly Anchor-driven**.

The Anchor is the authority for the Tutorial flow.

If:

```text
Topic|
Sub-Topic|
Level|
```

are blank, the Runtime should follow the Anchor/Test_Source strictly.

If one or more of these metadata fields are supplied, they become filters within the Anchor's permitted source scope.

Example:

```text
Test_Source| questions/gaca/1.txt
Topic|
Sub-Topic|
Level|
```

means:

```text
Strictly use questions/gaca/1.txt
→ eligible questions
→ select 10
```

Whereas:

```text
Test_Source| questions/gaca/1.txt
Topic| History
Sub-Topic| Ancient India
Level| 10
```

means:

```text
Anchor source = questions/gaca/1.txt
        +
Topic = History
        +
Sub-Topic = Ancient India
        +
Level = 10
        ↓
Eligible questions
        ↓
Select 10
```

### Architectural Principle

The Anchor remains the **authority / scope definition**.

The Knowledge Index is a **fast lookup/index layer**, not a replacement for the Anchor or the question TXT source.

---

# 3. Knowledge Index Architecture — Confirmed Reuse

An additional major architectural decision has now been confirmed.

A separate "temporary JSON" system should **not** be created.

The project already contains the Knowledge Index architecture:

```text
questions/*.txt
      ↓
services/questionLibraryEngine.js
      ↓
Knowledge Index Builder
      ↓
questionLibrary.<subject>.json
```

The existing generated Knowledge Library should be reused by:

```text
Admin Portal
Runtime
Tutorial filtering
Exam filtering
```

This avoids maintaining multiple parallel question indexes.

---

# 4. Existing `questionLibraryEngine.js` Assessment

The current engine already extracts global metadata fields including:

```text
Exam
Subject
Topic
SubTopic
Level
Notification
Type
Marks
QType
```

It also counts question blocks and detects the question format (`Q` / `QEN`).

It already aggregates files by Topic and records:

- total files
- total questions
- source files
- Sub-Topics
- Levels
- Exams

Therefore, the existing engine is a strong foundation and should be **upgraded**, not replaced.

---

# 5. Required Knowledge Index Upgrades

Two genuine limitations have now been identified.

## Upgrade A — Level-wise Question Counts

Current behavior records distinct Level values but does not calculate how many questions belong to each Level.

Current conceptual output:

```text
levels:
[
  "10",
  "12"
]
```

Required future capability:

```text
levels:
{
  "10": <question count>,
  "12": <question count>
}
```

The index should ultimately support counts at the relevant hierarchy:

```text
Subject
  ↓
Topic
  ↓
Sub-Topic
  ↓
Level
  ↓
Question Count
```

This will support both:

- Admin Portal statistics
- Runtime filtering

---

# 6. Required Metadata Resolution Upgrade

This is the second and more important Knowledge Index requirement.

The intended metadata hierarchy is:

## Global Metadata Has a Value

If a file contains:

```text
Topic| History
SubTopic|
Level| 10
```

then the global values are authoritative.

For each metadata field independently:

```text
Global value exists
        ↓
Use global value
        ↓
Ignore individual-question value
```

Therefore:

**Global metadata overrides individual metadata.**

---

## Global Metadata Is Blank

If the file contains:

```text
Topic|
SubTopic|
Level|
```

then the engine must inspect individual question metadata where available.

For example:

```text
Global:
Topic|
SubTopic|
Level|

Question 1:
Topic| History
SubTopic| Ancient India
Level| 10

Question 2:
Topic| Geography
SubTopic| Physical Geography
Level| 12
```

The effective metadata is question-specific.

---

## Metadata Resolution Must Be Per Field

This is important.

The fallback is not all-or-nothing.

Example:

```text
Global:
Topic| History
SubTopic|
Level| 10

Individual:
Topic| Geography
SubTopic| Ancient India
Level| 12
```

Effective metadata:

```text
Topic    → History       ← global wins
SubTopic → Ancient India ← global blank, use individual
Level    → 10            ← global wins
```

Therefore each metadata field independently follows:

```text
                 Metadata Resolution
                         │
              ┌──────────┴──────────┐
              │                     │
       Global value exists?    Global blank?
              │                     │
             YES                   YES
              │                     │
       Use global value       Check individual
       Ignore individual      question metadata
                                    │
                              Available?
                               /       \
                             YES        NO
                              │          │
                       Use individual   Blank
```

This rule applies at minimum to:

```text
Topic
SubTopic
Level
```

and should be preserved as the project metadata inheritance principle.

---

# 7. Important Current Limitation in `questionLibraryEngine.js`

The current engine stops global metadata scanning when it encounters the first:

```text
Q|
```

or:

```text
QEN|
```

question marker.

After the first question marker, later metadata is ignored by the current global metadata extractor.

That behavior is acceptable only for the existing file-level metadata model.

The engine must therefore be upgraded so that it can properly support the project's intended metadata inheritance model:

```text
Global metadata
        ↓
Question-level fallback only where global is blank
```

The upgrade must not break the existing global metadata behavior.

---

# 8. File-Level vs Question-Level Metadata — Required Design

The intended model is:

### File-level metadata

```text
Global metadata
      ↓
Applies to all questions
```

when a value exists.

### Question-level metadata

```text
Individual question metadata
      ↓
Used only when the corresponding global field is blank
```

This means the parser should not unnecessarily inspect individual metadata when the global value is already authoritative.

This is both an architectural rule and a performance consideration.

---

# 9. Admin Portal — Required Future Upgrade

The current Admin Portal shows aggregate section totals but does not yet expose Topic/Sub-Topic question counts.

The desired behavior is hierarchical and interactive:

```text
GACA — total
│
├── Topic A — count
│   ├── Sub-Topic A — count
│   └── Sub-Topic B — count
│
├── Topic B — count
│   └── Sub-Topic C — count
│
└── Topic C — count
```

### UI behavior

A Topic should be expandable/collapsible.

Example:

```text
GACA
  ▼ History ........ 180
      Ancient India .... 70
      Medieval India ... 55
      Modern India ..... 55

  ▶ Geography ........ 150

  ▶ Current Affairs .. 370
```

The Admin Portal should use the same generated Knowledge Index rather than creating a second statistics source.

---

# 10. Admin Portal / Runtime / Tutorial Shared Data Architecture

The preferred architecture is now:

```text
                  Question TXT
                 Source of Truth
                       │
                       ▼
             questionLibraryEngine
                       │
                       ▼
             Knowledge Index JSON
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
     Admin Portal    Runtime     Tutorial API
          │            │            │
      Statistics    Filtering    Filtering
```

The TXT files remain the source of truth.

The Knowledge Index JSON remains a generated/derived index.

No separate manually maintained temporary question-count JSON should be introduced unless a future requirement proves that the existing Knowledge Index cannot satisfy it.

---

# 11. Current Runtime Validation Status

### Validated / structurally compatible

```text
Real GACA Anchor TXT              ✅
anchorImporter.js                 ✅
gacaAnchorParser.js               ✅
candidateResolver.js              ✅
questionPoolBuilder.js            ✅
questionParser.js                 ✅
Existing GACA source file         ✅
Tutorial Corner source loading    ✅
```

### Not yet proven end-to-end

The new Runtime has not yet been formally proven through the complete live API → Quiz Portal path.

The important remaining distinction is:

```text
Existing Tutorial pipeline
        ≠
New Runtime pipeline
```

The fact that Tutorial Corner currently loads `questions/gaca/1.txt` correctly proves the existing Tutorial pipeline works, but it does not by itself prove that the new Runtime Engine is already connected to the live Quiz Portal.

---

# 12. API Integration — Current Position

`fetch-questions.js` is still a legacy API endpoint and currently does not call `runtimeEngine.js`.

`fetch-tutorial-questions.js` is also currently a legacy Tutorial API path.

Therefore:

**Do not delete either API.**

The intended architecture is:

```text
API endpoint
     ↓
Runtime Engine
     ↓
Anchor / criteria resolution
     ↓
Knowledge Index
     ↓
Candidate Resolution
     ↓
Question Pool
     ↓
Selection
     ↓
API response
```

The API endpoints remain the external entry points/adapters.

The Runtime Engine becomes the core execution layer.

---

# 13. Critical API Design Decision

Before modifying either API endpoint, the input contract must be defined.

For Tutorial Corner, the intended input is Anchor-driven.

For Exam Corner, the intended input remains criteria-driven and anchor-independent.

Therefore:

```text
Exam Corner
    → dynamic criteria

Tutorial Corner
    → standardized Anchor
```

These flows must not be forced into one identical request model.

---

# 14. Current Development State — 2026-08-11

### Completed

- ✅ Maintenance Framework v2.1 frozen
- ✅ YouTube Anchor Builder operational
- ✅ GACA Anchor Builder operational
- ✅ Mathematics Anchor Builder operational
- ✅ Topic/Sub-Topic/Level sequential preview behavior corrected
- ✅ Blank Topic/Sub-Topic/Level preservation corrected
- ✅ Step 13 Save & Prepare Download corrected
- ✅ Anchor TXT download verified
- ✅ Real GACA Anchor TXT generated and inspected
- ✅ Anchor importer/runtime adapter chain inspected
- ✅ Candidate Resolver inspected
- ✅ Question Pool Builder inspected
- ✅ GACA question parser contract inspected
- ✅ Existing Tutorial Corner source-file loading confirmed
- ✅ `fetch-questions.js` vs `fetch-tutorial-questions.js` roles clarified
- ✅ Existing Knowledge Index architecture identified as reusable
- ✅ `questionLibraryEngine.js` inspected
- ✅ Level-count requirement identified
- ✅ Global-vs-individual metadata inheritance requirement identified
- ✅ Admin Portal Topic → Sub-Topic hierarchy requirement recorded

---

# 15. Current Work — Not Yet Implemented

The following are **requirements/assessments**, not completed code changes:

1. Upgrade `questionLibraryEngine.js` to support question-level metadata fallback.
2. Add Level-wise question counts to the Knowledge Index.
3. Ensure Topic/Sub-Topic/Level counts are correctly represented in the generated Knowledge Index.
4. Upgrade Admin Portal statistics to expose Topic → Sub-Topic hierarchy.
5. Make Admin Portal Topics expandable/collapsible.
6. Define the exact Tutorial Runtime filtering behavior using Anchor + Knowledge Index.
7. Integrate the new Runtime Engine with the appropriate API endpoints.
8. Perform live end-to-end Runtime validation through the actual Quiz Portal.

---

# 16. Immediate Next Step

Before modifying the API layer, the next development task is:

## Knowledge Index Engine Upgrade

Target file:

```text
services/questionLibraryEngine.js
```

Required upgrades:

### A. Level counts

Add question counts by Level.

### B. Metadata inheritance

Implement:

```text
Global metadata value
        ↓
authoritative
```

and:

```text
Global metadata blank
        ↓
inspect individual question metadata
        ↓
use individual value if available
```

Apply this independently to:

```text
Topic
SubTopic
Level
```

### C. Preserve existing behavior

Do not break:

- existing global metadata extraction
- existing question counting
- existing Q / QEN detection
- existing curriculum validation
- existing Knowledge Index output contract unless the extension is required
- existing Maintenance tools
- existing Runtime modules

---

# 17. After Knowledge Index Upgrade

The planned order is:

```text
1. Upgrade questionLibraryEngine.js
        ↓
2. Regenerate / inspect Knowledge Index JSON
        ↓
3. Verify Topic/Sub-Topic/Level counts
        ↓
4. Upgrade Admin Portal statistics
        ↓
5. Verify expandable Topic → Sub-Topic UI
        ↓
6. Connect Runtime to Knowledge Index filtering
        ↓
7. Define Tutorial Anchor filtering
        ↓
8. Integrate fetch-tutorial-questions.js
        ↓
9. Integrate fetch-questions.js
        ↓
10. Live Quiz Portal Runtime validation
```

Do not skip the Knowledge Index validation before API migration.

---

# 18. Engineering Safety Rules for the Next Phase

- **Do not delete the existing master Project Status history.**
- **Do not replace the mother copy with a shortened summary.**
- Preserve previous milestones and decisions.
- Append new assessments and corrections.
- Treat the latest verified state as the baseline.
- Do not modify the frozen Anchor Builder unless a regression is found.
- Do not delete legacy API endpoints during Runtime migration.
- Do not create a duplicate temporary question index unless technically necessary.
- Prefer extending the existing Knowledge Index architecture.
- Keep TXT question files as the source of truth.
- Keep generated JSON as a derived/index layer.
- Do not let global and individual metadata conflict silently; global values must win.
- When global metadata is blank, individual question metadata may supply the missing field.
- Resolve Topic, SubTopic and Level independently.
- Validate each architectural boundary before modifying the next layer.

---

# 19. Master Continuation Point

## **Current Phase: Knowledge Index Engine Upgrade**

We are no longer at the original "Anchor Builder Integration" stage.

The Anchor Builder has been successfully stabilized and frozen.

We are now at:

```text
Anchor Builder
      ↓
Real Anchor TXT
      ↓
Runtime architecture inspected
      ↓
Knowledge Index architecture inspected
      ↓
★ CURRENT: Upgrade Knowledge Index Engine
      ↓
Admin Portal hierarchy
      ↓
Runtime filtering
      ↓
API Integration
      ↓
Live Quiz Portal validation
```

### Next file to modify/review

```text
services/questionLibraryEngine.js
```

### Next architectural objective

Implement the project's agreed metadata inheritance rules and Level-wise question counts without losing the existing Knowledge Index functionality.

---

# Session Checkpoint — 2026-08-11

**Master status is preserved and updated.**

The project should resume from:

> **Knowledge Index Engine Upgrade — Level Counts + Global/Individual Metadata Resolution**

No previous project history should be discarded when creating the next master copy.
